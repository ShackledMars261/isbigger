from setuptools import setup, find_packages

setup(
    name="isbigger",
    packages=find_packages(include=["isbigger"]),
    version="0.1",
    description="A package to compare two integers",
    author="Jack Martin"
)